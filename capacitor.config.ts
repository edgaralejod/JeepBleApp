import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.jeepble',
  appName: 'JeepBleApp',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
